import React from 'react';
import Layout from "../components/Layout/Layout";

const Brands = () => {
    return (
        <Layout>
            Brands Page
        </Layout>
    );
};

export default Brands;