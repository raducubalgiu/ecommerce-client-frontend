import React from 'react';
import Layout from "../components/Layout/Layout";

const Products = () => {
    return (
        <Layout>
            Products Page
        </Layout>
    );
};

export default Products;